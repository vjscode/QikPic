<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- the theme applied to the application or activity -->
    <style name="ActionBarTheme"
        parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/ActionBar</item>
    </style>

    <!-- ActionBar styles -->
    <style name="ActionBar"
        parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/actionbar_background</item>
    </style>

    <!-- the theme applied to the application or activity for showing window overlay -->
    <style name="QikPicOverlayActionBarTheme"
        parent="@android:style/Theme.Holo.Light.DarkActionBar">
        <item name="android:windowActionBarOverlay">true</item>
    </style>

    <style name="TagDialogTheme" parent="@android:style/Theme.Panel">
        <item name="android:windowAnimationStyle">@style/TagDialog.Window</item>
    </style>

    <style name="TagDialog.Window" parent="@android:style/Animation.Activity">
        <item name="android:windowEnterAnimation">@anim/tag_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/tag_dialog_out</item>
    </style>


    <style name="TagDialogWindowAnimation">
        <item name="android:windowEnterAnimation">@anim/tag_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/tag_dialog_out</item>
    </style>

    <style name="QikPicActionBarTheme"
        parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>

    <style name="MyActionBar"
        parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="background">@color/actionbar_background</item>
    </style>
</resources>